version: "3.9"

services:

  ingestion-service:
    build:
      context: ../../
      dockerfile: infrastructure/docker/Dockerfile.ingestion
    container_name: tracpy-ingestion
    ports:
      - "4000:4000"
    environment:
      NODE_ENV: development
    volumes:
      - ../../:/app
      - /app/node_modules